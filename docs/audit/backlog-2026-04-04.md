# 全量审核 backlog — 2026-04-04

本轮按「Superpowers + SKILL.md」计划执行：**阶段 A–F** 静态审阅 + **一项 P1 契约修复** 与 **文档对账**。集成/生产验证仍依赖真实 MySQL/Redis（见 [code-audit-log.md](code-audit-log.md) Batch 6）。

## 已核实 / 已关闭

| ID | 类型 | 说明 | 证据 / 动作 |
|----|------|------|-------------|
| A-1 | 文档 | Controller 清单与代码不一致（缺 2 个、计数 29→31） | 已更新 [controller-inventory.md](controller-inventory.md) |
| A-2 |  Prep | 项目级审计 SKILL | 已新增 [`.cursor/skills/lantuconnect-backend-audit/SKILL.md`](../../.cursor/skills/lantuconnect-backend-audit/SKILL.md) |
| B-1 | 闭环 | 平台角色旧码 | 仅 [AuthServiceImpl](../../src/main/java/com/lantu/connect/auth/service/impl/AuthServiceImpl.java) 内 `normalizeRole` 保留 `consumer`→`user`、`dept_admin`→`reviewer`，属兼容而非漂移 |
| C-1 | 契约 | `BusinessException` 若干业务码映射为 HTTP **500** | **已修复**：`REJECT_REASON_REQUIRED`、`GRANT_APPLICATION_NOT_FOUND`、数据集/系统角色权限、重复收藏/版本、Grant 工单等与 REST 语义对齐 — [GlobalExceptionHandler](../../src/main/java/com/lantu/connect/common/exception/GlobalExceptionHandler.java) `resolveHttpStatus` |
| D-1 | 展示口径 | `GET /dashboard/admin-overview` 的 `summary` 键含义不清 | 已在 [DashboardController](../../src/main/java/com/lantu/connect/dashboard/controller/DashboardController.java) `adminOverview` Javadoc 标注各字段数据源与 `call_log` 口径 |
| F-1 | 验证 | 回归测试 | `mvn test` 通过（2026-04-04，会话复验通过） |
| E-0 | 持久化 spot-check | `access_policy` 与 JDBC | Java（`ResourceRegistryServiceImpl` 等）与 [`sql/lantu_connect.sql`](../../sql/lantu_connect.sql)、[`V6__resource_access_policy.sql`](../../sql/incremental/V6__resource_access_policy.sql)、[`migrations/20260403_resource_access_policy.sql`](../../sql/migrations/20260403_resource_access_policy.sql) 均为 `VARCHAR(32) NOT NULL DEFAULT 'grant_required'` — 本轮未见字面漂移；其余列仍以「DDL 变更后全仓 grep」为准 |

## 仍待观察 / P2（未改代码）

| ID | 类型 | 说明 | 建议 |
|----|------|------|------|
| E-1 | 持久化 | `t_resource` 无 MP 实体，大量 JDBC 字符串 | Schema 变更后全仓 `grep` SQL；保持与 [sql/](../../sql) 同步流程 |
| E-2 | 配置 | `integration-mock`、Mock 短信、GeoIP 等 | 部署 Checklist 显式关闭模拟并填外联 URL（沿用 code-audit-log 汇总） |
| C-2 | 契约 | 其余 `ResultCode`（如 `INTERNAL_ERROR` 5001）仍映射 500 | 按需细分；前端已依赖 `body.code` 时可不改 HTTP |
| D-2 | 展示 | 列表接口「仅 ID、缺 displayName」 | 按接口逐个 grep `UserDisplayNameResolver`；与前端表格列对齐 |

## 分页契约（阶段 C 抽样）

标准类型：[PageResult](../../src/main/java/com/lantu/connect/common/result/PageResult.java)：字段 `list`、`total`、`page`、`pageSize` — 与多数列表接口一致；若某处自建 Map 需单独标注。

## 参考

- 静态基线：[code-audit-log.md](code-audit-log.md)（2026-03-31）
- 路由清单：[controller-inventory.md](controller-inventory.md)
