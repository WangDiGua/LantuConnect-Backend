# 双读切换与回滚演练

## 目标
- 本文记录“过渡期切换方案”及其演练历史。
- 当前项目已进入最终态：只保留新模型读取，不再保留旧模型切换。

## 当前状态（最终态）
- 统一网关固定读取 `t_resource*` 新模型。
- 过渡期 `dual_compare/legacy_only` 逻辑已移除。
- 旧资源模型与旧治理表已物理删除。

## 一致性校验 SQL
- 文件：按 `sql/migrations/README.md` 规范在 `sql/migrations/` 维护一致性校验脚本（当前基线为 `sql/lantu_connect.sql`）。
- 覆盖内容：
  - 各资源类型新旧行数比对
  - `resource_code` 集合缺失检查
  - 扩展表行数比对
  - 监控治理新旧表行数比对

## 最终态回滚原则
- 回滚通过数据库备份与发布回退实现，不再依赖旧模型切换开关。
- 对账脚本保留，用于发布前后一致性核验。
