-- 已废弃：920xx 号段种子与「演示-*」部门会与正式账号混杂。
-- 请改用同目录下 reset_coherent_identity_data.sql
-- （在保留 admin/dept_admin/developer/testuser 的基础上清理并归一化全库身份数据）。
SELECT 'use reset_coherent_identity_data.sql instead' AS hint;
